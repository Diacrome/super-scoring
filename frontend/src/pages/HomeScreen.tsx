import React, {FC} from 'react';

const HomeScreen: FC = () => {
    return (
        <div>
            Необходимо указать id теста в адресной строке
        </div>
    );
};

export default HomeScreen;
